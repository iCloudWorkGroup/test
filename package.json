{
  "name": "example",
  "version": "0.0.6",
  "main": "script.js",
  "scripts": {
    "test":"istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info  && cat ./coverage/lcov.info | coveralls && rm -rf ./coverage"
  },
  "dependencies":{
    "grunt-bump":"^0.7.0",
    "mocha": "^2.1.0",
    "istanbul": "^0.3.2",
    "coveralls": "2.12.0",
    "expect.js": "^0.3.1",
    "grunt-conventional-changelog":"^6.1.0",
    "grunt-contrib-uglify":"^2.1.0",
    "load-grunt-tasks":"0.4.0",
    "grunt-contrib-jshint": "~0.11.1",
    "grunt-contrib-less":"1.3.0",
    "requirejs":"2.1.9"
  },
  "devDependencies": {
    "grunt": "0.4.1"
  }
}
